[tool.poetry]
name = "smartcargo-aipa"
version = "0.1.0"
description = "Backend SmartCargo-AIPA"
authors = ["Maykel Rodriguez <maykel75122805321@gmail.com>"]
packages = [{include = "src"}]

[tool.poetry.dependencies]
python = "^3.12"
flask = "^2.4"
sqlalchemy = "^2.0"
stripe = "^10.0"
google-genai = "^0.1"

[tool.poetry.dev-dependencies]
# opcionales: tests, linters, etc.
